# Profile visuals

The profile embeds local PNG files instead of external stats cards. Project previews are reused from the linked public repositories. Solar uses a repository schematic preview; Laura is browser mock mode; Hermes and SpellScroll are existing captures; ILCI uses an editorial title graphic rather than an unverified performance claim.
